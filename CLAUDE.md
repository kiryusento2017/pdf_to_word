# pdf_to_word

PDF 转 Word 的桌面软件。Electron 外壳 + Python 后端 + MinerU 识别。
给老师用的，转的是理科讲义 —— **公式必须是 Word 原生公式**（能双击编辑那种），
这是整个项目存在的理由，别的都可以让步。

---

## 动手前先读哪份

| 要干什么 | 读哪份 |
|---|---|
| 改某个模块 | 那个 `.py` 文件**开头的模块 docstring** —— 这个项目把「为什么这么写」全记在代码里，比任何外部文档都准 |
| 想知道某个设计为什么长这样 | `docs/DESIGN.md` |
| 发版 | `docs/RELEASE.md` —— 🔴 **从头读到尾，别检索式跳读**（见下面第 2 条） |
| 看待办、决策台账、历史事故 | `docs/PLAN_NEXT.md` |
| 仓库结构、怎么跑起来 | `README.md` |

---

## 五条一定会踩的坑

都是真踩过的，不是假想。

### 1. 跑测试必须用 `.venv\Scripts\python.exe`

```
.venv\Scripts\python.exe -m unittest discover -s tests -q
node tests\front_check.js
```

（具体多少条以 `README.md` 为准 —— 那份有 `check_docs.py` 盯着，
这份没有，所以这里不写数字，免得过期了没人发现。）

用全局 `python` 会得到 **9 个假失败**（`test_server` 整个导入失败、
`test_开发环境是CUDA版`、`test_读得出本地装的版本` …）—— 因为 torch 和
mineru 只装在 `.venv` 里。这几条测的是「本机真实环境」，缺依赖时它们
**应该**红。看见这一组一起红，先确认用的哪个 python，别当成真 bug 去查。

（2026-09-05 全量审查时一上来就栽在这儿。）

### 2. 发版前 `RELEASE.md` 要从头读完

那份文件第 3 行就写着「每次发版前从头照着走一遍，别凭记忆」。
它有八节，**跳着读必然漏**。

2026-09-05 发 v0.2.0 时就漏了第五节「发布说明写什么」，结果发布说明写成
散文开头、`---` 放在末尾 —— 而软件的 `update.split_notes()` 认第一条
独占一行的 `---`，之前的算摘要塞进 110px 的框里。摘要变成了 78 行 1384 字符。

正确格式（`RELEASE.md` 第五节）：

```
- 新增 关于页和环境检测页
- 修改 Word 正文字体改成宋体
- 修复 一份 PDF 出问题会带倒整批

---

## 详细说明
……
```

摘要三到五条，每条以「新增 / 修改 / 修复」开头。

### 3. `dist/PDF2Word/` 不是源码，而且会严重落后

它是**发行版产物目录**。`--sfx` 打的是它当前的内容，不是源码。

2026-09-05 发版前查出来：它还停在 v0.1.1，`maint.py` / `deps.py` /
`upgrade.py` 三个模块**根本不在里面**，上一轮 13 项改动一样没同步。
差一点就只复制了改动的三个文件了事。

打包前必须全量同步（命令见 `RELEASE.md`「手工改过发行版目录的话」），
🔴 **前端的目标是 `resources\app\`，不是 `app\`** —— 同步到 `app\` 软件
读不到，而且不报错，安静地跑旧前端。

同步完必须验：

```
diff -rq pipeline dist/PDF2Word/pipeline
diff -rq app/renderer dist/PDF2Word/resources/app/renderer
```

### 4. 测试绿 ≠ 代码对

`maint.note_run()` 读 `formulas_ok` / `formulas_src`，而 `convert` 传下来的
rep 里叫 `formulas` / `formulas_xsl` —— 诊断报告那一格**永远是 `?/?`**。
当时 403 条测试**全绿**，因为那条测试自己手工编了一个用 `formulas_ok` 的 rep：
**测试和实现一起错，于是一起绿**。

写测试时，构造的数据结构必须跟真实调用方**同一个来源**。手编 dict 之前
先去看生产方到底吐什么字段。

### 5. 行尾是**混着的**，`sed -i` 会把整个文件冲掉

不是全 CRLF 也不是全 LF —— `pipeline/*.py`、`app/renderer/*.js`、
`tests/front_check.js` 是 CRLF，而 `tests/test_update.py`、`README.md`、
`docs/*.md` 是 LF。**别凭印象猜，动手前先跑这条**（把 <文件> 换成目标）：

```
.venv\Scripts\python.exe -c "import io,sys;b=io.open(sys.argv[1],'rb').read();c=b.count(bytes([13,10]));print('CRLF' if c else 'LF', c, b.count(bytes([10]))-c)" <文件>
```

两个坑，2026-09-05 一次踩了两个：

🔴 **`sed -i` 会把 CRLF 全换成 LF。** 于是 `git diff` 显示整文件几百行全变，
真正的改动被埋在里面看不见。`PLAN_NEXT.md` 里记着同一个坑的第一次
（改第三方版权文件时踩的），当时的结论是「动第三方文件必须用二进制方式改」——
**范围说小了，这个项目里所有文件都得这么改。**

🔴 **Git Bash 里用 grep 数回车判断行尾会骗你。** 那个 shell 里的 `$'\r'` 没被
解释成回车，变成空模式匹配了所有行，于是「CR 数 == 总行数」，看着像
「全是 CRLF」而实际不是。`head -c 4000 | grep -q` 那种写法同理，还会因为
只看前 4000 字节而漏掉后面。**判行尾只信上面那条 Python 命令。**

正确姿势：读写都带 `newline=''`（保留原样），锚点字符串按目标文件的实际行尾
转换后再匹配，并且 `assert` 锚点命中数 == 1 —— 命中 0 次通常就是行尾没对上。

🔴 **判断该用哪种行尾时，别写「只要含 CRLF 就当 CRLF」这种判据。**
文件一旦混了（比如上一次脚本写坏，留下几个真回车），这种判据立刻返回真，
于是新内容全按 CRLF 写进去，越修越乱。要数哪种多，混合时就报错停下。
2026-09-05 写这一条的时候就是这么把这份 CLAUDE.md 自己搞混的：
原本纯 LF 145 行，硬塞进 32 行 CRLF。

改完对着 `git diff --stat` 看一眼：**只有你动的那几行才对**。
整个文件几百行全变 = 行尾被冲了，回滚重来。

⚠️ 还有一层：**写这类替换脚本时，heredoc 会吃掉一层反斜杠**，
双写的回车换行传进去会退化成单写，非 raw 字符串一解析就成了真回车，
把要写入的命令行拦腰切断。脚本里凡是含反斜杠的文本，一律放进
Python 的 raw 三引号字符串里，别用普通字符串。

---

## 这个项目的几条硬规矩

违反了会被直接打回，因为每一条背后都有事故：

- **不承诺不存在的行为。** 话术里说「失败了会自动退回 CPU」，而代码里
  根本没有回退逻辑 —— 用户会按那个预期做决定，然后在最花时间的地方白等。
- **查不到就说「查不到」，绝不显示「已是最新」。** 这两个意思差很远，
  混了就是假绿灯。同理：没测过的速度那一列就是空的，不拿别的数字顶替。
- **判失败不等于删产物。** 转一份四分钟，因为一个公式没转成就把 132 张图、
  18 个表、612 个转好的公式一起扔掉，代价太大。改名留在原地，让人一眼
  分得清正品次品。
- **记日志绝不能把主流程搞崩。** 转换主路径上的写文件一律 try 包住。
- **不吞掉「有产物但没跑完」。** MinerU 边处理边写，第七页崩掉时前六页
  已经落盘 —— 找得到 `.md` 不等于成功，退出码必须看。
- **路径来自前端就必须验。** server 只绑 127.0.0.1，但那不等于只有我们能连。
- 🔴 **`D:\claude_code_workspace\pdf转word的实验文件夹}}》》==··` 是小蔡自己的
  装机副本，他自己升级，不要插手。** 不许替他跑更新、不许改那边的
  `version.json`、不许往那个目录同步代码或覆盖文件。
  读它是可以的 —— 查日志、读装了哪些包、拿它的 `runtime\python` 跑只读
  验证（比如模拟老用户 `check_requires`），这些都不改变它的状态。
  界线就一条：**只读，不写。**

---

## 五个自动检查（前四个发版前，第五个转正后）

```
.venv\Scripts\python.exe tools\check_docs.py       文档里的数字跟代码对不对得上
.venv\Scripts\python.exe tools\check_claims.py     文档说的功能代码里有没有
.venv\Scripts\python.exe tools\check_package.py    打出来的包干不干净
.venv\Scripts\python.exe tools\check_upstream.py   依赖上游有没有新版（会拦打包）
.venv\Scripts\python.exe tools\check_release.py    发出去之后 GitHub 上的状态对不对
```

前四个在发版**之前**跑，`check_release` 在**转正之后**跑 —— 它查的是
GitHub 侧那些「页面上看着正常、用户直接受害、还不报错」的状态
（latest 没指过来、tag 指错 commit、附件缺一个或传残了、发布说明没有
分隔线）。2026-09-05 发 v0.2.2 时就栽在第一条上。

这个项目遇到「靠人记得」的问题，一律的解法是**把它变成一条能跑的命令**。
加规矩之前先想想能不能变成检查。

---

## 复用的审查工具

`D:\claude_code_workspace\_scratch\` 下留着几个扫描器，2026-09-05 全量审查
写的，其中后两个是真正挖出 bug 的：

| 脚本 | 查什么 |
|---|---|
| `pdf2word_scan.py` | 多维静态扫描（死 import、裸 except、硬编码、吞异常…） |
| `pdf2word_fields.py` | **字段生产/消费交叉** —— 挖出了上面第 4 条那个 bug |
| `pdf2word_contract.py` | **「不抛异常」契约检查** —— 挖出 `extract.run` 的漏洞 |
| `pdf2word_doccheck.py` | 文档里指到的路径存不存在、版本号分布 |
